{
  "name": "openglove",
  "private": true,
  "devDependencies": {
    "pnpm": "^10.30.1",
    "rollup": "^4",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "bootstrap": "pnpm -w install",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  }
}
